name: TIDY Runner Smoke

on:
  push:
    branches:
      - tidy-apk-build-v0.2-temp
    paths:
      - ".github/workflows/tidy-runner-smoke.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TIDY runner OK"
